entorno en jupyterlab: python -m ipykernel install --user --name=epa-kernel --display-name "EPA Visualization"

pip install dash

mkdir epa-visualization
cd epa-visualization
python -m venv venv


pip install requirements.txr

